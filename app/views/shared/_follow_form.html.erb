<% unless current_user?(@user) %>
  <span id="follow_form" class="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'shared/unfollow' %>
  <% else %>
    <%= render 'shared/follow' %>
  <% end %>
  </span>
<% end %>